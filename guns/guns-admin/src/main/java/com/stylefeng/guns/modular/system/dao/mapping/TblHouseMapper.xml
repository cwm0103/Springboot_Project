<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.modular.system.dao.TblHouseMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.stylefeng.guns.modular.system.model.TblHouse">
        <result column="id" property="id" />
        <result column="hour_user" property="hourUser" />
        <result column="hour_address" property="hourAddress" />
        <result column="hour_data" property="hourData" />
        <result column="hour_desc" property="hourDesc" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, hour_user AS hourUser, hour_address AS hourAddress, hour_data AS hourData, hour_desc AS hourDesc
    </sql>

</mapper>
